import { FC } from 'react';

import { {{pascalCase name}}ViewProps } from './{{pascalCase name}}.props';

const {{pascalCase name}}View: FC<{{pascalCase name}}ViewProps> = (props) => (
  <div className="flex">{{pascalCase name}}</div>
);

export default {{pascalCase name}}View;
